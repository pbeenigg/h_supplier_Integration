version: '3.8'

name:  heytrip-supplier-integration
services:
  # HeyTrip Supplier Integration Service
  heytrip-supplier-service:
    build: .
    container_name: heytrip-supplier-integration
    environment:
      # Database Configuration
      DB_URL: jdbc:p6spy:mysql://pc-j6c1oer259zv98w65.rwlb.rds.aliyuncs.com:3306/supplier_pax?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
      DB_USERNAME: xwd_pax_0389516
      DB_PASSWORD: lJfGo#Kgj$6H29y!1q&0SpDf4G*oi


      # Application Configuration
      APP_ID: heytrip_supplier_integration_pax
      APP_SECRET: HeyTrip@Pax#SupplierIntegration!2025

      SERVER_NAME: heytrip-supplier-integration
      SERVER_PATH: /pax/api/xiwanSupplier/supp
      SERVER_PORT: 8080
      SPRING_PROFILES_ACTIVE: prod
      ENCRYPTION_KEY: 427ae41e4649b934ca495991b7852b855
      
      # JVM Options
      JAVA_OPTS: -Xmx2g -Xms1g -XX:+UseG1GC
    ports:
      - "8080:8080"
    networks:
      - heytrip-supplier-network
    volumes:
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  # Nginx
  nginx:
    image: nginx:alpine
    container_name:  heytrip-supplier-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - heytrip-supplier-integration
    networks:
      - heytrip-supplier-network
    restart: unless-stopped

networks:
  heytrip-supplier-network:
    driver: bridge
